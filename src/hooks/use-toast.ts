
import { toast as toastFunction } from "../components/ui/use-toast";
import { useToast as useToastFunction } from "../components/ui/use-toast";

export const toast = toastFunction;
export const useToast = useToastFunction;
