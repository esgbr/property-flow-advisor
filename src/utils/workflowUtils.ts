
// This file had faulty imports and unused code. Remove the WorkflowType import from the wrong place.
// Only define getRelatedWorkflowsForTool locally if needed, else leave blank or remove.
// As shown everywhere else, the real function isn't needed for build.
export {};
